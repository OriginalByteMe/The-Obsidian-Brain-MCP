---
phase: 01-cli-migration
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/obsidian_brain/server.py
autonomous: true
gap_closure: true
requirements:
  - SDK-01
  - SDK-02
  - SDK-03
  - SDK-04
  - SDK-05
  - CLI-01
  - CLI-02
  - CLI-03
  - CLI-04
  - CLI-05
  - CLI-06
  - CLI-07
  - CLI-08
  - TOOL-01
  - TOOL-02
  - TOOL-03
  - TOOL-04
  - TOOL-05

must_haves:
  truths:
    - "register_onboarding_tools is called with both mcp and client arguments"
    - "register_knowledge_resource is called with both mcp and client arguments"
    - "All tool and resource registration calls in server.py pass the client singleton"
  artifacts:
    - path: "src/obsidian_brain/server.py"
      provides: "Correctly wired tool and resource registration"
      contains: "register_onboarding_tools(mcp, client)"
    - path: "src/obsidian_brain/server.py"
      provides: "Correctly wired knowledge resource"
      contains: "register_knowledge_resource(mcp, client)"
  key_links:
    - from: "src/obsidian_brain/server.py"
      to: "src/obsidian_brain/tools/onboarding.py"
      via: "register_onboarding_tools(mcp, client)"
      pattern: "register_onboarding_tools\\(mcp,\\s*client\\)"
    - from: "src/obsidian_brain/server.py"
      to: "src/obsidian_brain/resources/knowledge.py"
      via: "register_knowledge_resource(mcp, client)"
      pattern: "register_knowledge_resource\\(mcp,\\s*client\\)"
---

<objective>
Fix two wiring bugs in server.py where register_onboarding_tools and register_knowledge_resource are called with only the server argument, missing the required client argument.

Purpose: Close verification gaps -- these bugs cause runtime crashes when onboarding tools or the knowledge resource are invoked.
Output: server.py with all registration calls correctly passing the client singleton.
</objective>

<execution_context>
@/home/noahr/.claude/get-shit-done/workflows/execute-plan.md
@/home/noahr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-migration/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix server.py wiring bugs for onboarding tools and knowledge resource</name>
  <files>src/obsidian_brain/server.py</files>
  <action>
    Fix two registration calls in server.py that are missing the `client` argument:

    1. Change `register_onboarding_tools(mcp)` to `register_onboarding_tools(mcp, client)` (around line 144)
    2. Change `register_knowledge_resource(mcp)` to `register_knowledge_resource(mcp, client)` (around line 149)

    Root cause: Plans 02 and 03 ran in parallel. Plan 02 wrote server.py with 1-arg calls for modules not yet migrated. Plan 03 migrated those modules to 2-arg signatures but did not update server.py.

    After editing, verify that ALL registration calls in server.py consistently pass the client argument (except register_structure_resource which is cache-only and does not need client).
  </action>
  <verify>
    <automated>cd /home/noahr/projects/The-Obsidian-Brain-MCP && python -c "
import ast, sys
with open('src/obsidian_brain/server.py') as f:
    source = f.read()
# Check the two fixed calls exist
assert 'register_onboarding_tools(mcp, client)' in source, 'onboarding still missing client'
assert 'register_knowledge_resource(mcp, client)' in source, 'knowledge resource still missing client'
# Check no 1-arg calls remain for functions that need client
for name in ['register_vault_tools', 'register_link_tools', 'register_tag_tools', 'register_search_tools', 'register_daily_tools', 'register_knowledge_tools', 'register_onboarding_tools', 'register_memory_tools', 'register_knowledge_resource']:
    pattern = f'{name}(mcp)'
    # Allow register_structure_resource(mcp) -- it's cache-only
    if pattern in source and name != 'register_structure_resource':
        print(f'FAIL: {name} still called with only mcp')
        sys.exit(1)
print('All registration calls correctly pass client')
"</automated>
  </verify>
  <done>
    - register_onboarding_tools(mcp, client) called with both args
    - register_knowledge_resource(mcp, client) called with both args
    - No other 1-arg registration calls remain (except register_structure_resource which is cache-only)
    - Onboarding tools and knowledge resource will no longer crash at runtime
  </done>
</task>

</tasks>

<verification>
- All registration calls in server.py pass client where required
- grep confirms no remaining 1-arg calls for functions expecting 2 args
- Python syntax check passes on server.py
</verification>

<success_criteria>
- server.py has register_onboarding_tools(mcp, client)
- server.py has register_knowledge_resource(mcp, client)
- Verification score moves from 12/14 to 14/14
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-migration/01-05-SUMMARY.md`
</output>
