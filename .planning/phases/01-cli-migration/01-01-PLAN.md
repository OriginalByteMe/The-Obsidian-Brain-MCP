---
phase: 01-cli-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/obsidian_brain/protocol.py
  - src/obsidian_brain/exceptions.py
  - src/obsidian_brain/cli_client.py
  - src/obsidian_brain/parsers.py
  - tests/conftest.py
  - tests/test_protocol.py
  - tests/test_cli_client.py
  - tests/test_parsers.py
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03, CLI-04, CLI-05, CLI-06, CLI-08, SDK-05]

must_haves:
  truths:
    - "VaultClient Protocol defines the async interface with all methods that have CLI equivalents"
    - "ObsidianCLIClient implements VaultClient Protocol using asyncio.create_subprocess_exec"
    - "All subprocess calls have explicit timeouts via asyncio.wait_for"
    - "CLI binary is located via shutil.which with OBSIDIAN_CLI_PATH env var override"
    - "Note paths are safe from command injection (list-form exec, no shell=True)"
    - "CLI JSON output is parsed in a dedicated parsers module, not inline"
    - "Shared exceptions (CLIError, NoteNotFoundError) are in a dedicated module"
  artifacts:
    - path: "src/obsidian_brain/protocol.py"
      provides: "VaultClient Protocol with ~14 async methods"
      exports: ["VaultClient"]
    - path: "src/obsidian_brain/cli_client.py"
      provides: "ObsidianCLIClient implementing VaultClient"
      exports: ["ObsidianCLIClient"]
    - path: "src/obsidian_brain/parsers.py"
      provides: "CLI JSON output parsing functions"
      exports: ["parse_note_read", "parse_file_list", "parse_search_results", "parse_tags", "parse_daily"]
    - path: "src/obsidian_brain/exceptions.py"
      provides: "Shared exception hierarchy"
      exports: ["ObsidianCLIError", "NoteNotFoundError", "CLITimeoutError", "CLINotFoundError"]
    - path: "tests/conftest.py"
      provides: "Shared test fixtures for mocked CLI subprocess"
    - path: "tests/test_cli_client.py"
      provides: "Unit tests for CLI client with mocked subprocess"
    - path: "tests/test_protocol.py"
      provides: "Protocol conformance tests"
    - path: "tests/test_parsers.py"
      provides: "Parser unit tests with sample CLI JSON"
  key_links:
    - from: "src/obsidian_brain/cli_client.py"
      to: "src/obsidian_brain/protocol.py"
      via: "implements VaultClient Protocol"
      pattern: "class ObsidianCLIClient.*VaultClient"
    - from: "src/obsidian_brain/cli_client.py"
      to: "src/obsidian_brain/parsers.py"
      via: "calls parser functions on CLI stdout"
      pattern: "from .parsers import"
    - from: "src/obsidian_brain/cli_client.py"
      to: "src/obsidian_brain/exceptions.py"
      via: "raises CLIError on non-zero exit"
      pattern: "from .exceptions import"
---

<objective>
Create the foundation layer for the CLI migration: VaultClient Protocol, ObsidianCLIClient implementation, CLI output parsers, shared exceptions, and test infrastructure.

Purpose: Everything else in the migration depends on these contracts. The Protocol defines the interface all tool modules will code against. The CLI client is the sole backend. The parsers isolate CLI output handling. Tests validate the foundation before building on top of it.

Output: Protocol, CLI client, parsers, exceptions, and full test suite with mocked subprocess calls.
</objective>

<execution_context>
@/home/noahr/.claude/get-shit-done/workflows/execute-plan.md
@/home/noahr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-migration/01-CONTEXT.md
@.planning/phases/01-cli-migration/01-RESEARCH.md

@src/obsidian_brain/client.py
@src/obsidian_brain/models.py

<interfaces>
<!-- Current ObsidianClient methods that map to CLI equivalents (from client.py): -->

```python
# These are the methods that have CLI equivalents and must appear in VaultClient Protocol:
async def list_directory(self, path: str = "/") -> list[dict[str, Any]]: ...
async def get_all_files(self, path: str = "/") -> list[str]: ...
async def get_note(self, path: str, include_metadata: bool = True) -> dict[str, Any]: ...
async def note_exists(self, path: str) -> bool: ...
async def create_note(self, path: str, content: str) -> None: ...
async def update_note(self, path: str, content: str) -> None: ...
async def append_to_note(self, path: str, content: str) -> None: ...
async def delete_note(self, path: str) -> None: ...
async def search_simple(self, query: str, context_length: int = 100) -> list[dict[str, Any]]: ...
async def get_periodic(self, period: str = "daily", date: str | None = None) -> dict[str, Any]: ...
async def append_periodic(self, content: str, period: str = "daily", date: str | None = None) -> None: ...

# DROPPED (no CLI equivalent): patch_note, search_dql, search_jsonlogic, get_periodic(non-daily), update_periodic, get_server_info
# NEW CLI-only methods to add: get_tags, get_backlinks, get_links
```

<!-- CLI command mapping from RESEARCH.md: -->
```
list_directory  -> obsidian files folder="{path}" format=json
get_all_files   -> obsidian files ext=md format=json
get_note        -> obsidian read path="{path}" format=json
create_note     -> obsidian create name="{name}" path="{folder}" content="{content}" --silent
update_note     -> obsidian create name="{name}" path="{folder}" content="{content}" --silent --overwrite
append_to_note  -> obsidian append file="{name}" content="{content}"
delete_note     -> obsidian delete file="{name}"
search_simple   -> obsidian search query="{query}" format=json
get_daily_note  -> obsidian daily:read format=json
append_daily    -> obsidian daily:append content="{content}"
get_tags        -> obsidian tags format=json
get_backlinks   -> obsidian backlinks file="{name}" format=json
get_links       -> obsidian links file="{name}" format=json
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create VaultClient Protocol, exceptions, and parsers with tests</name>
  <files>
    src/obsidian_brain/protocol.py,
    src/obsidian_brain/exceptions.py,
    src/obsidian_brain/parsers.py,
    tests/conftest.py,
    tests/test_protocol.py,
    tests/test_parsers.py
  </files>
  <behavior>
    - Protocol: ObsidianCLIClient satisfies VaultClient Protocol (structural typing check)
    - Protocol: All ~14 methods are async and match signatures from the interfaces block above
    - Exceptions: CLIError stores returncode, stderr, command; NoteNotFoundError inherits from CLIError; CLITimeoutError and CLINotFoundError are distinct
    - Parsers: parse_note_read converts CLI JSON to {path, content, tags, frontmatter, modified} dict
    - Parsers: parse_file_list converts CLI JSON to list of file path strings
    - Parsers: parse_search_results converts CLI JSON to list of {path, matches, score} dicts
    - Parsers: parse_tags converts CLI JSON to {tag: count} dict
    - Parsers: parse_daily converts CLI JSON to {content, tags, frontmatter} dict
    - Parsers: all parsers handle empty/missing fields gracefully (return defaults, not crash)
  </behavior>
  <action>
    1. Create `src/obsidian_brain/exceptions.py`:
       - `ObsidianCLIError(Exception)` with `returncode: int`, `stderr: str`, `command: list[str]`
       - `NoteNotFoundError(ObsidianCLIError)` with `path: str`
       - `CLITimeoutError(ObsidianCLIError)` for subprocess timeout
       - `CLINotFoundError(Exception)` for binary-not-found at startup (not a CLI execution error)

    2. Create `src/obsidian_brain/protocol.py`:
       - `VaultClient(Protocol)` with all methods from the interfaces block
       - Rename periodic methods to be daily-specific: `get_daily_note(date: str | None = None)` and `append_daily(content: str, date: str | None = None)` (per user decision: daily only)
       - Add: `get_tags() -> dict[str, int]`, `get_backlinks(path: str) -> list[str]`, `get_links(path: str) -> list[str]`
       - Total: ~14 methods, all async, all with `...` body

    3. Create `src/obsidian_brain/parsers.py`:
       - One function per CLI command output shape
       - Each function takes raw JSON string or parsed dict, returns normalized Python dict/list
       - Since exact CLI JSON shapes are an open question (see RESEARCH.md), make parsers defensive: use `.get()` with defaults, handle both possible key names
       - Include docstrings showing expected input/output shapes

    4. Create `tests/conftest.py`:
       - `mock_cli_output` fixture: helper to create fake subprocess results (stdout bytes, stderr bytes, returncode)
       - `sample_note_json` fixture: sample CLI JSON for a note read
       - `sample_file_list_json` fixture: sample CLI JSON for file listing
       - `sample_search_json` fixture: sample CLI JSON for search results
       - `sample_tags_json` fixture: sample CLI JSON for tags
       - Remove any pytest-httpx usage; add pytest-timeout to dev deps if not present

    5. Create `tests/test_protocol.py`:
       - Test that VaultClient Protocol has all expected method names
       - Test structural typing: a class implementing all methods satisfies isinstance check via runtime_checkable

    6. Create `tests/test_parsers.py`:
       - Test each parser function with sample CLI JSON from fixtures
       - Test each parser with empty/malformed input (graceful defaults)
  </action>
  <verify>
    <automated>cd /home/noahr/projects/The-Obsidian-Brain-MCP && python -m pytest tests/test_protocol.py tests/test_parsers.py -x -v --timeout=30</automated>
  </verify>
  <done>VaultClient Protocol exported with ~14 async methods, exceptions module with 4 exception classes, parsers module with 5+ parse functions, all tests green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ObsidianCLIClient with mocked subprocess tests</name>
  <files>
    src/obsidian_brain/cli_client.py,
    tests/test_cli_client.py
  </files>
  <behavior>
    - Binary detection: finds obsidian via shutil.which, respects OBSIDIAN_CLI_PATH env var
    - Binary detection: raises CLINotFoundError with diagnostic message if not found
    - _run method: uses asyncio.create_subprocess_exec with list-form args (never shell=True)
    - _run method: pipes stdout and stderr
    - _run method: applies timeout via asyncio.wait_for (default 30s)
    - _run method: raises CLITimeoutError on timeout, kills process
    - _run method: raises ObsidianCLIError on non-zero returncode
    - _run_json method: appends format=json and parses stdout as JSON
    - get_note: calls `obsidian read path="{path}" format=json`, returns parsed note dict
    - create_note: calls `obsidian create` with name, path, content, --silent flag
    - update_note: calls `obsidian create` with --overwrite --silent flags
    - delete_note: calls `obsidian delete file="{name}"`
    - search_simple: calls `obsidian search query="{query}" format=json`
    - get_daily_note: calls `obsidian daily:read format=json`
    - append_daily: calls `obsidian daily:append content="{content}"`
    - get_tags: calls `obsidian tags format=json`
    - get_backlinks: calls `obsidian backlinks file="{name}" format=json`
    - get_links: calls `obsidian links file="{name}" format=json`
    - Path sanitization: validates no null bytes in paths
    - ObsidianCLIClient satisfies VaultClient Protocol
  </behavior>
  <action>
    Create `src/obsidian_brain/cli_client.py`:
    - `ObsidianCLIClient` class with `__init__(cli_path: str | None = None, vault: str | None = None, timeout: float = 30.0)`
    - `find_cli_binary()` module-level function: checks `OBSIDIAN_CLI_PATH` env var, then `shutil.which("obsidian")`, raises `CLINotFoundError` with full diagnostic message (binary path searched, link to Obsidian 1.12+ download)
    - `async _run(*args, timeout=None) -> str`: core subprocess method using `asyncio.create_subprocess_exec` with `stdout=PIPE, stderr=PIPE`, wrapped in `asyncio.wait_for`. On timeout: `proc.kill()` then raise `CLITimeoutError`. On non-zero exit: raise `ObsidianCLIError` with returncode, stderr, command.
    - `async _run_json(*args, timeout=None) -> Any`: calls `_run` with `format=json` appended, returns `json.loads(stdout)`
    - Implement all ~14 VaultClient Protocol methods by delegating to `_run` / `_run_json` with appropriate CLI commands
    - For `create_note`/`update_note`: split path into folder and name components (CLI uses `name=` and `path=` separately)
    - Path sanitization: reject null bytes, use list-form exec (inherently safe from shell injection)

    Create `tests/test_cli_client.py`:
    - Mock `asyncio.create_subprocess_exec` to return fake Process objects
    - Test binary detection with mocked shutil.which and env vars
    - Test _run with successful output, non-zero exit, timeout
    - Test each VaultClient method builds correct CLI command args
    - Test path sanitization rejects null bytes
    - Test Protocol conformance: `isinstance(ObsidianCLIClient(...), VaultClient)` with runtime_checkable
  </action>
  <verify>
    <automated>cd /home/noahr/projects/The-Obsidian-Brain-MCP && python -m pytest tests/test_cli_client.py -x -v --timeout=30</automated>
  </verify>
  <done>ObsidianCLIClient implements VaultClient Protocol, all subprocess calls use async exec with timeouts, binary detection works with env var and PATH, all tests green</done>
</task>

</tasks>

<verification>
```bash
cd /home/noahr/projects/The-Obsidian-Brain-MCP && python -m pytest tests/test_protocol.py tests/test_parsers.py tests/test_cli_client.py -x -v --timeout=30
```

Additional checks:
- `grep -r "shell=True" src/` returns nothing
- `grep -r "subprocess.run" src/` returns nothing
- `python -c "from obsidian_brain.protocol import VaultClient; print('OK')"` succeeds
- `python -c "from obsidian_brain.cli_client import ObsidianCLIClient; print('OK')"` succeeds
</verification>

<success_criteria>
- VaultClient Protocol defines the full async interface (~14 methods)
- ObsidianCLIClient implements all Protocol methods using subprocess exec
- All subprocess calls use list-form exec (no shell=True), have timeouts, pipe stdout/stderr
- CLI binary detection works via PATH and OBSIDIAN_CLI_PATH override
- Parsers handle all CLI JSON output shapes with defensive defaults
- Exceptions have proper hierarchy with useful diagnostic messages
- Full test suite passes with mocked subprocess (no real CLI calls)
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-migration/01-01-SUMMARY.md`
</output>
